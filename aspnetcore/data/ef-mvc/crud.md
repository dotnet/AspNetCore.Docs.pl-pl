---
title: 'Samouczek: Implementowanie funkcji CRUD - ASP.NET MVC z EF Core'
description: W tym samouczku przejrzysz i dostosujesz kod CRUD (tworzenie, odczyt, aktualizacja, usuwanie), który szkieletowanie MVC automatycznie tworzy dla Ciebie w kontrolerach i widokach.
author: rick-anderson
ms.author: riande
ms.custom: mvc
ms.date: 02/04/2019
ms.topic: tutorial
uid: data/ef-mvc/crud
ms.openlocfilehash: 2aa4ef48509b9a34f3b25eb657b1ecac51c1374b
ms.sourcegitcommit: f7886fd2e219db9d7ce27b16c0dc5901e658d64e
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/06/2020
ms.locfileid: "79416213"
---
# <a name="tutorial-implement-crud-functionality---aspnet-mvc-with-ef-core"></a><span data-ttu-id="713da-103">Samouczek: Implementowanie funkcji CRUD - ASP.NET MVC z EF Core</span><span class="sxs-lookup"><span data-stu-id="713da-103">Tutorial: Implement CRUD Functionality - ASP.NET MVC with EF Core</span></span>

<span data-ttu-id="713da-104">W poprzednim samouczku utworzono aplikację MVC, która przechowuje i wyświetla dane przy użyciu entity framework i SQL Server LocalDB.</span><span class="sxs-lookup"><span data-stu-id="713da-104">In the previous tutorial, you created an MVC application that stores and displays data using the Entity Framework and SQL Server LocalDB.</span></span> <span data-ttu-id="713da-105">W tym samouczku przejrzysz i dostosujesz kod CRUD (tworzenie, odczyt, aktualizacja, usuwanie), który szkieletowanie MVC automatycznie tworzy dla Ciebie w kontrolerach i widokach.</span><span class="sxs-lookup"><span data-stu-id="713da-105">In this tutorial, you'll review and customize the CRUD (create, read, update, delete) code that the MVC scaffolding automatically creates for you in controllers and views.</span></span>

> [!NOTE]
> <span data-ttu-id="713da-106">Jest powszechną praktyką, aby zaimplementować wzorzec repozytorium w celu utworzenia warstwy abstrakcji między kontrolerem a warstwą dostępu do danych.</span><span class="sxs-lookup"><span data-stu-id="713da-106">It's a common practice to implement the repository pattern in order to create an abstraction layer between your controller and the data access layer.</span></span> <span data-ttu-id="713da-107">Aby te samouczki były proste i skoncentrowane na nauczaniu, jak korzystać z samej struktury encji, nie używają repozytoriów.</span><span class="sxs-lookup"><span data-stu-id="713da-107">To keep these tutorials simple and focused on teaching how to use the Entity Framework itself, they don't use repositories.</span></span> <span data-ttu-id="713da-108">Aby uzyskać informacje na temat repozytoriów z EF, zobacz [ostatni samouczek z tej serii](advanced.md).</span><span class="sxs-lookup"><span data-stu-id="713da-108">For information about repositories with EF, see [the last tutorial in this series](advanced.md).</span></span>

<span data-ttu-id="713da-109">W tym samouczku zostały wykonane następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="713da-109">In this tutorial, you:</span></span>

> [!div class="checklist"]
> * <span data-ttu-id="713da-110">Dostosowywanie strony Szczegóły</span><span class="sxs-lookup"><span data-stu-id="713da-110">Customize the Details page</span></span>
> * <span data-ttu-id="713da-111">Aktualizowanie strony Tworzenie</span><span class="sxs-lookup"><span data-stu-id="713da-111">Update the Create page</span></span>
> * <span data-ttu-id="713da-112">Aktualizowanie strony edycji</span><span class="sxs-lookup"><span data-stu-id="713da-112">Update the Edit page</span></span>
> * <span data-ttu-id="713da-113">Aktualizowanie strony Usuwanie</span><span class="sxs-lookup"><span data-stu-id="713da-113">Update the Delete page</span></span>
> * <span data-ttu-id="713da-114">Zamykanie połączeń z bazą danych</span><span class="sxs-lookup"><span data-stu-id="713da-114">Close database connections</span></span>

## <a name="prerequisites"></a><span data-ttu-id="713da-115">Wymagania wstępne</span><span class="sxs-lookup"><span data-stu-id="713da-115">Prerequisites</span></span>

* [<span data-ttu-id="713da-116">Wprowadzenie do EF Core i ASP.NET Core MVC</span><span class="sxs-lookup"><span data-stu-id="713da-116">Get started with EF Core and ASP.NET Core MVC</span></span>](intro.md)

## <a name="customize-the-details-page"></a><span data-ttu-id="713da-117">Dostosowywanie strony Szczegóły</span><span class="sxs-lookup"><span data-stu-id="713da-117">Customize the Details page</span></span>

<span data-ttu-id="713da-118">Szkieletowy kod dla students index strony pominął `Enrollments` właściwość, ponieważ ta właściwość posiada kolekcję.</span><span class="sxs-lookup"><span data-stu-id="713da-118">The scaffolded code for the Students Index page left out the `Enrollments` property, because that property holds a collection.</span></span> <span data-ttu-id="713da-119">Na stronie **Szczegóły** zawartość kolekcji zostanie wyświetlona w tabeli HTML.</span><span class="sxs-lookup"><span data-stu-id="713da-119">In the **Details** page, you'll display the contents of the collection in an HTML table.</span></span>

<span data-ttu-id="713da-120">W *Controllers/StudentsController.cs*metoda akcji dla widoku Szczegóły `SingleOrDefaultAsync` używa metody do `Student` pobierania pojedynczej jednostki.</span><span class="sxs-lookup"><span data-stu-id="713da-120">In *Controllers/StudentsController.cs*, the action method for the Details view uses the `SingleOrDefaultAsync` method to retrieve a single `Student` entity.</span></span> <span data-ttu-id="713da-121">Dodaj kod, `Include`który wywołuje .</span><span class="sxs-lookup"><span data-stu-id="713da-121">Add code that calls `Include`.</span></span> <span data-ttu-id="713da-122">`ThenInclude`i `AsNoTracking` metody, jak pokazano w poniższym wyróżnionym kodzie.</span><span class="sxs-lookup"><span data-stu-id="713da-122">`ThenInclude`,  and `AsNoTracking` methods, as shown in the following highlighted code.</span></span>

[!code-csharp[](intro/samples/cu/Controllers/StudentsController.cs?name=snippet_Details&highlight=8-12)]

<span data-ttu-id="713da-123">I `Include` `ThenInclude` metody spowodować kontekst załadować właściwość `Student.Enrollments` nawigacji i `Enrollment.Course` w ramach każdej rejestracji właściwości nawigacji.</span><span class="sxs-lookup"><span data-stu-id="713da-123">The `Include` and `ThenInclude` methods cause the context to load the `Student.Enrollments` navigation property, and within each enrollment the `Enrollment.Course` navigation property.</span></span>  <span data-ttu-id="713da-124">Dowiesz się więcej o tych metodach w samouczku [odczytu powiązanych danych.](read-related-data.md)</span><span class="sxs-lookup"><span data-stu-id="713da-124">You'll learn more about these methods in the [read related data](read-related-data.md) tutorial.</span></span>

<span data-ttu-id="713da-125">Metoda `AsNoTracking` zwiększa wydajność w scenariuszach, w których zwrócone jednostki nie będą aktualizowane w okresie istnienia bieżącego kontekstu.</span><span class="sxs-lookup"><span data-stu-id="713da-125">The `AsNoTracking` method improves performance in scenarios where the entities returned won't be updated in the current context's lifetime.</span></span> <span data-ttu-id="713da-126">Dowiesz się więcej `AsNoTracking` na temat na końcu tego samouczka.</span><span class="sxs-lookup"><span data-stu-id="713da-126">You'll learn more about `AsNoTracking` at the end of this tutorial.</span></span>

### <a name="route-data"></a><span data-ttu-id="713da-127">Dane trasy</span><span class="sxs-lookup"><span data-stu-id="713da-127">Route data</span></span>

<span data-ttu-id="713da-128">Wartość klucza przekazywana `Details` do metody pochodzi z *danych trasy*.</span><span class="sxs-lookup"><span data-stu-id="713da-128">The key value that's passed to the `Details` method comes from *route data*.</span></span> <span data-ttu-id="713da-129">Dane trasy to dane znalezione przez spinacz modelu w segmencie adresu URL.</span><span class="sxs-lookup"><span data-stu-id="713da-129">Route data is data that the model binder found in a segment of the URL.</span></span> <span data-ttu-id="713da-130">Na przykład trasa domyślna określa segmenty kontrolera, akcji i identyfikatora:</span><span class="sxs-lookup"><span data-stu-id="713da-130">For example, the default route specifies controller, action, and id segments:</span></span>

[!code-csharp[](intro/samples/cu/Startup.cs?name=snippet_Route&highlight=5)]

<span data-ttu-id="713da-131">W poniższym adresie URL domyślna trasa mapuje instruktora jako kontrolera, Indeks jako akcję i 1 jako identyfikator; są to wartości danych trasy.</span><span class="sxs-lookup"><span data-stu-id="713da-131">In the following URL, the default route maps Instructor as the controller, Index as the action, and 1 as the id; these are route data values.</span></span>

```
http://localhost:1230/Instructor/Index/1?courseID=2021
```

<span data-ttu-id="713da-132">Ostatnia część adresu URL ("?courseID=2021") jest wartością ciągu zapytania.</span><span class="sxs-lookup"><span data-stu-id="713da-132">The last part of the URL ("?courseID=2021") is a query string value.</span></span> <span data-ttu-id="713da-133">Spinacza modelu przekaże również wartość `Index` identyfikatora do parametru metody, `id` jeśli przekażesz go jako wartość ciągu zapytania:</span><span class="sxs-lookup"><span data-stu-id="713da-133">The model binder will also pass the ID value to the `Index` method `id` parameter if you pass it as a query string value:</span></span>

```
http://localhost:1230/Instructor/Index?id=1&CourseID=2021
```

<span data-ttu-id="713da-134">Na stronie Indeks adresy URL hiperłączy są tworzone przez instrukcje pomocnika znaczników w widoku Razor.</span><span class="sxs-lookup"><span data-stu-id="713da-134">In the Index page, hyperlink URLs are created by tag helper statements in the Razor view.</span></span> <span data-ttu-id="713da-135">W poniższym kodzie `id` Razor parametr jest `id` zgodny z trasą domyślną, więc jest dodawany do danych trasy.</span><span class="sxs-lookup"><span data-stu-id="713da-135">In the following Razor code, the `id` parameter matches the default route, so `id` is added to the route data.</span></span>

```html
<a asp-action="Edit" asp-route-id="@item.ID">Edit</a>
```

<span data-ttu-id="713da-136">Spowoduje to wygenerowanie `item.ID` następującego kodu HTML w przypadku 6:</span><span class="sxs-lookup"><span data-stu-id="713da-136">This generates the following HTML when `item.ID` is 6:</span></span>

```html
<a href="/Students/Edit/6">Edit</a>
```

<span data-ttu-id="713da-137">W poniższym kodzie `studentID` Razor nie pasuje do parametru w trasie domyślnej, więc jest dodawany jako ciąg zapytania.</span><span class="sxs-lookup"><span data-stu-id="713da-137">In the following Razor code, `studentID` doesn't match a parameter in the default route, so it's added as a query string.</span></span>

```html
<a asp-action="Edit" asp-route-studentID="@item.ID">Edit</a>
```

<span data-ttu-id="713da-138">Spowoduje to wygenerowanie `item.ID` następującego kodu HTML w przypadku 6:</span><span class="sxs-lookup"><span data-stu-id="713da-138">This generates the following HTML when `item.ID` is 6:</span></span>

```html
<a href="/Students/Edit?studentID=6">Edit</a>
```

<span data-ttu-id="713da-139">Aby uzyskać więcej informacji na <xref:mvc/views/tag-helpers/intro>temat pomocników tagów, zobacz .</span><span class="sxs-lookup"><span data-stu-id="713da-139">For more information about tag helpers, see <xref:mvc/views/tag-helpers/intro>.</span></span>

### <a name="add-enrollments-to-the-details-view"></a><span data-ttu-id="713da-140">Dodawanie rejestracji do widoku Szczegóły</span><span class="sxs-lookup"><span data-stu-id="713da-140">Add enrollments to the Details view</span></span>

<span data-ttu-id="713da-141">Otwórz *widoki/Studenci/Szczegóły.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="713da-141">Open *Views/Students/Details.cshtml*.</span></span> <span data-ttu-id="713da-142">Każde pole jest `DisplayNameFor` wyświetlane `DisplayFor` przy użyciu i pomocników, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="713da-142">Each field is displayed using `DisplayNameFor` and `DisplayFor` helpers, as shown in the following example:</span></span>

[!code-html[](intro/samples/cu/Views/Students/Details.cshtml?range=13-18&highlight=2,5)]

<span data-ttu-id="713da-143">Po ostatnim polu i bezpośrednio `</dl>` przed tagiem zamknięcia dodaj następujący kod, aby wyświetlić listę rejestracji:</span><span class="sxs-lookup"><span data-stu-id="713da-143">After the last field and immediately before the closing `</dl>` tag, add the following code to display a list of enrollments:</span></span>

[!code-html[](intro/samples/cu/Views/Students/Details.cshtml?range=31-52)]

<span data-ttu-id="713da-144">Jeśli wcięcie kodu jest nieprawidłowe po wklejeniu kodu, naciśnij klawisz CTRL-K-D, aby go poprawić.</span><span class="sxs-lookup"><span data-stu-id="713da-144">If code indentation is wrong after you paste the code, press CTRL-K-D to correct it.</span></span>

<span data-ttu-id="713da-145">Ten kod pętli za pośrednictwem `Enrollments` jednostek we właściwości nawigacji.</span><span class="sxs-lookup"><span data-stu-id="713da-145">This code loops through the entities in the `Enrollments` navigation property.</span></span> <span data-ttu-id="713da-146">Dla każdej rejestracji wyświetla tytuł kursu i klasę.</span><span class="sxs-lookup"><span data-stu-id="713da-146">For each enrollment, it displays the course title and the grade.</span></span> <span data-ttu-id="713da-147">Tytuł kursu jest pobierany z Course jednostki, `Course` która jest przechowywana we właściwości nawigacji enrollments jednostki.</span><span class="sxs-lookup"><span data-stu-id="713da-147">The course title is retrieved from the Course entity that's stored in the `Course` navigation property of the Enrollments entity.</span></span>

<span data-ttu-id="713da-148">Uruchom aplikację, wybierz kartę **Uczniowie** i kliknij **łącze Szczegóły** dla ucznia.</span><span class="sxs-lookup"><span data-stu-id="713da-148">Run the app, select the **Students** tab, and click the **Details** link for a student.</span></span> <span data-ttu-id="713da-149">Zobaczysz listę kursów i ocen dla wybranego ucznia:</span><span class="sxs-lookup"><span data-stu-id="713da-149">You see the list of courses and grades for the selected student:</span></span>

![Strona Szczegóły ucznia](crud/_static/student-details.png)

## <a name="update-the-create-page"></a><span data-ttu-id="713da-151">Aktualizowanie strony Tworzenie</span><span class="sxs-lookup"><span data-stu-id="713da-151">Update the Create page</span></span>

<span data-ttu-id="713da-152">W *StudentsController.cs*, zmodyfikuj metodę HttpPost, `Create` dodając blok try-catch i usuwając identyfikator z atrybutu. `Bind`</span><span class="sxs-lookup"><span data-stu-id="713da-152">In *StudentsController.cs*, modify the HttpPost `Create` method by adding a try-catch block and removing ID from the `Bind` attribute.</span></span>

[!code-csharp[](intro/samples/cu/Controllers/StudentsController.cs?name=snippet_Create&highlight=4,6-7,14-21)]

<span data-ttu-id="713da-153">Ten kod dodaje Student jednostki utworzone przez ASP.NET Core Model Spinacz do zestawu jednostek Studenci, a następnie zapisuje zmiany w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="713da-153">This code adds the Student entity created by the ASP.NET Core MVC model binder to the Students entity set and then saves the changes to the database.</span></span> <span data-ttu-id="713da-154">(Spinacz modelu odnosi się do funkcji ASP.NET Core MVC, która ułatwia pracę z danymi przesłanych przez formularz; spinacz modelu konwertuje zaksięgowane wartości formularzy na typy CLR i przekazuje je do metody akcji w parametrach.</span><span class="sxs-lookup"><span data-stu-id="713da-154">(Model binder refers to the ASP.NET Core MVC functionality that makes it easier for you to work with data submitted by a form; a model binder converts posted form values to CLR types and passes them to the action method in parameters.</span></span> <span data-ttu-id="713da-155">W takim przypadku spinacz modelu tworzy wystąpienia jednostki Student dla Ciebie przy użyciu wartości właściwości z kolekcji Form.)</span><span class="sxs-lookup"><span data-stu-id="713da-155">In this case, the model binder instantiates a Student entity for you using property values from the Form collection.)</span></span>

<span data-ttu-id="713da-156">Usunięto `ID` z atrybutu, `Bind` ponieważ identyfikator jest wartością klucza podstawowego, którą sql server ustawi automatycznie po wstawieniu wiersza.</span><span class="sxs-lookup"><span data-stu-id="713da-156">You removed `ID` from the `Bind` attribute because ID is the primary key value which SQL Server will set automatically when the row is inserted.</span></span> <span data-ttu-id="713da-157">Dane wejściowe od użytkownika nie ustawia wartość identyfikatora.</span><span class="sxs-lookup"><span data-stu-id="713da-157">Input from the user doesn't set the ID value.</span></span>

<span data-ttu-id="713da-158">Inne niż `Bind` atrybut, try-catch bloku jest jedyną zmianą, którą dokonano do szkieletu kodu.</span><span class="sxs-lookup"><span data-stu-id="713da-158">Other than the `Bind` attribute, the try-catch block is the only change you've made to the scaffolded code.</span></span> <span data-ttu-id="713da-159">Jeśli wyjątek, który `DbUpdateException` pochodzi z jest przechwytywany podczas zapisywania zmian, wyświetlany jest ogólny komunikat o błędzie.</span><span class="sxs-lookup"><span data-stu-id="713da-159">If an exception that derives from `DbUpdateException` is caught while the changes are being saved, a generic error message is displayed.</span></span> <span data-ttu-id="713da-160">`DbUpdateException`wyjątki są czasami spowodowane przez coś zewnętrznego do aplikacji, a nie błąd programowania, więc użytkownik powinien spróbować ponownie.</span><span class="sxs-lookup"><span data-stu-id="713da-160">`DbUpdateException` exceptions are sometimes caused by something external to the application rather than a programming error, so the user is advised to try again.</span></span> <span data-ttu-id="713da-161">Chociaż nie zostały zaimplementowane w tym przykładzie, aplikacja jakości produkcji będzie rejestrować wyjątek.</span><span class="sxs-lookup"><span data-stu-id="713da-161">Although not implemented in this sample, a production quality application would log the exception.</span></span> <span data-ttu-id="713da-162">Aby uzyskać więcej informacji, zobacz **sekcję Dziennik dla wglądu** w [monitorowanie i telemetrię (Tworzenie rzeczywistych aplikacji w chmurze za pomocą platformy Azure).](/aspnet/aspnet/overview/developing-apps-with-windows-azure/building-real-world-cloud-apps-with-windows-azure/monitoring-and-telemetry)</span><span class="sxs-lookup"><span data-stu-id="713da-162">For more information, see the **Log for insight** section in [Monitoring and Telemetry (Building Real-World Cloud Apps with Azure)](/aspnet/aspnet/overview/developing-apps-with-windows-azure/building-real-world-cloud-apps-with-windows-azure/monitoring-and-telemetry).</span></span>

<span data-ttu-id="713da-163">Atrybut `ValidateAntiForgeryToken` pomaga zapobiegać atakom fałszerstwa żądań między witrynami (CSRF).</span><span class="sxs-lookup"><span data-stu-id="713da-163">The `ValidateAntiForgeryToken` attribute helps prevent cross-site request forgery (CSRF) attacks.</span></span> <span data-ttu-id="713da-164">Token jest automatycznie wstrzykiwany do widoku przez [FormTagHelper](xref:mvc/views/working-with-forms#the-form-tag-helper) i jest uwzględniany podczas przesyłania formularza przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="713da-164">The token is automatically injected into the view by the [FormTagHelper](xref:mvc/views/working-with-forms#the-form-tag-helper) and is included when the form is submitted by the user.</span></span> <span data-ttu-id="713da-165">Token jest sprawdzany `ValidateAntiForgeryToken` przez atrybut.</span><span class="sxs-lookup"><span data-stu-id="713da-165">The token is validated by the `ValidateAntiForgeryToken` attribute.</span></span> <span data-ttu-id="713da-166">Aby uzyskać więcej informacji na temat CSRF, zobacz [Forgery przeciw żądaniem](../../security/anti-request-forgery.md).</span><span class="sxs-lookup"><span data-stu-id="713da-166">For more information about CSRF, see [Anti-Request Forgery](../../security/anti-request-forgery.md).</span></span>

<a id="overpost"></a>

### <a name="security-note-about-overposting"></a><span data-ttu-id="713da-167">Uwaga dotycząca zabezpieczeń dotycząca nadpostywania</span><span class="sxs-lookup"><span data-stu-id="713da-167">Security note about overposting</span></span>

<span data-ttu-id="713da-168">Atrybut, `Bind` który szkieletowidek zawiera `Create` w metodzie jest jednym ze sposobów ochrony przed overposting w scenariuszach tworzenia.</span><span class="sxs-lookup"><span data-stu-id="713da-168">The `Bind` attribute that the scaffolded code includes on the `Create` method is one way to protect against overposting in create scenarios.</span></span> <span data-ttu-id="713da-169">Załóżmy na przykład, `Secret` że jednostka Student zawiera właściwość, której nie chcesz ustawiać tej strony sieci Web.</span><span class="sxs-lookup"><span data-stu-id="713da-169">For example, suppose the Student entity includes a `Secret` property that you don't want this web page to set.</span></span>

```csharp
public class Student
{
    public int ID { get; set; }
    public string LastName { get; set; }
    public string FirstMidName { get; set; }
    public DateTime EnrollmentDate { get; set; }
    public string Secret { get; set; }
}
```

<span data-ttu-id="713da-170">Nawet jeśli nie masz `Secret` pola na stronie internetowej, haker może użyć narzędzia, takiego jak Fiddler, `Secret` lub napisać javascript, aby opublikować wartość formularza.</span><span class="sxs-lookup"><span data-stu-id="713da-170">Even if you don't have a `Secret` field on the web page, a hacker could use a tool such as Fiddler, or write some JavaScript, to post a `Secret` form value.</span></span> <span data-ttu-id="713da-171">Bez `Bind` atrybutu ograniczającego pola używane przez spinacza modelu podczas tworzenia wystąpienia studenta, `Secret` spinacz modelu będzie odebrać tę wartość formularza i użyć go do utworzenia wystąpienia jednostki Student.</span><span class="sxs-lookup"><span data-stu-id="713da-171">Without the `Bind` attribute limiting the fields that the model binder uses when it creates a Student instance, the model binder would pick up that `Secret` form value and use it to create the Student entity instance.</span></span> <span data-ttu-id="713da-172">Następnie niezależnie od wartości hakera określonego dla pola `Secret` formularza zostaną zaktualizowane w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="713da-172">Then whatever value the hacker specified for the `Secret` form field would be updated in your database.</span></span> <span data-ttu-id="713da-173">Na poniższej ilustracji przedstawiono `Secret` narzędzie Fiddler dodające pole (z wartością "OverPost") do zaksięgowanych wartości formularza.</span><span class="sxs-lookup"><span data-stu-id="713da-173">The following image shows the Fiddler tool adding the `Secret` field (with the value "OverPost") to the posted form values.</span></span>

![Skrzypek dodający pole Tajne](crud/_static/fiddler.png)

<span data-ttu-id="713da-175">Wartość "OverPost" zostanie następnie pomyślnie `Secret` dodana do właściwości wstawionego wiersza, chociaż nigdy nie zamierzałeś, aby strona sieci web mogła ustawić tę właściwość.</span><span class="sxs-lookup"><span data-stu-id="713da-175">The value "OverPost" would then be successfully added to the `Secret` property of the inserted row, although you never intended that the web page be able to set that property.</span></span>

<span data-ttu-id="713da-176">Można zapobiec overposting w scenariuszach edycji, najpierw odczytując `TryUpdateModel`jednostkę z bazy danych, a następnie wywołując , przekazując na liście jawnych dozwolonych właściwości.</span><span class="sxs-lookup"><span data-stu-id="713da-176">You can prevent overposting in edit scenarios by reading the entity from the database first and then calling `TryUpdateModel`, passing in an explicit allowed properties list.</span></span> <span data-ttu-id="713da-177">Jest to metoda używana w tych samouczkach.</span><span class="sxs-lookup"><span data-stu-id="713da-177">That's the method used in these tutorials.</span></span>

<span data-ttu-id="713da-178">Alternatywnym sposobem zapobiegania overposting, który jest preferowany przez wielu deweloperów jest użycie modeli widoku, a nie klasy jednostek z powiązaniem modelu.</span><span class="sxs-lookup"><span data-stu-id="713da-178">An alternative way to prevent overposting that's preferred by many developers is to use view models rather than entity classes with model binding.</span></span> <span data-ttu-id="713da-179">Uwzględnij tylko właściwości, które chcesz zaktualizować w modelu widoku.</span><span class="sxs-lookup"><span data-stu-id="713da-179">Include only the properties you want to update in the view model.</span></span> <span data-ttu-id="713da-180">Po zakończeniu spinacza modelu MVC skopiuj właściwości modelu widoku do wystąpienia jednostki, opcjonalnie używając narzędzia, takiego jak AutoMapper.</span><span class="sxs-lookup"><span data-stu-id="713da-180">Once the MVC model binder has finished, copy the view model properties to the entity instance, optionally using a tool such as AutoMapper.</span></span> <span data-ttu-id="713da-181">Użyj `_context.Entry` w wystąpieniu jednostki, `Unchanged`aby ustawić `Property("PropertyName").IsModified` jego stan na , a następnie ustawić wartość true dla każdej właściwości jednostki, która jest uwzględniona w modelu widoku.</span><span class="sxs-lookup"><span data-stu-id="713da-181">Use `_context.Entry` on the entity instance to set its state to `Unchanged`, and then set `Property("PropertyName").IsModified` to true on each entity property that's included in the view model.</span></span> <span data-ttu-id="713da-182">Ta metoda działa zarówno w edytować i tworzyć scenariusze.</span><span class="sxs-lookup"><span data-stu-id="713da-182">This method works in both edit and create scenarios.</span></span>

### <a name="test-the-create-page"></a><span data-ttu-id="713da-183">Testowanie strony Tworzenie</span><span class="sxs-lookup"><span data-stu-id="713da-183">Test the Create page</span></span>

<span data-ttu-id="713da-184">Kod w *widokach/uczniach/create.cshtml* używa `label`, `input`i `span` (dla wiadomości sprawdzania poprawności) pomocników tagów dla każdego pola.</span><span class="sxs-lookup"><span data-stu-id="713da-184">The code in *Views/Students/Create.cshtml* uses `label`, `input`, and `span` (for validation messages) tag helpers for each field.</span></span>

<span data-ttu-id="713da-185">Uruchom aplikację, wybierz kartę **Uczniowie** i kliknij pozycję **Utwórz nowy**.</span><span class="sxs-lookup"><span data-stu-id="713da-185">Run the app, select the **Students** tab, and click **Create New**.</span></span>

<span data-ttu-id="713da-186">Wprowadź nazwy i datę.</span><span class="sxs-lookup"><span data-stu-id="713da-186">Enter names and a date.</span></span> <span data-ttu-id="713da-187">Spróbuj wprowadzić nieprawidłową datę, jeśli przeglądarka ci na to pozwala.</span><span class="sxs-lookup"><span data-stu-id="713da-187">Try entering an invalid date if your browser lets you do that.</span></span> <span data-ttu-id="713da-188">(Niektóre przeglądarki zmuszają do używania selektora dat). Następnie kliknij przycisk **Utwórz,** aby wyświetlić komunikat o błędzie.</span><span class="sxs-lookup"><span data-stu-id="713da-188">(Some browsers force you to use a date picker.) Then click **Create** to see the error message.</span></span>

![Błąd sprawdzania poprawności daty](crud/_static/date-error.png)

<span data-ttu-id="713da-190">Jest to sprawdzanie poprawności po stronie serwera, które są domyślnie ujmowany; w późniejszym samouczku zobaczysz, jak dodać atrybuty, które wygenerują kod do sprawdzania poprawności po stronie klienta.</span><span class="sxs-lookup"><span data-stu-id="713da-190">This is server-side validation that you get by default; in a later tutorial you'll see how to add attributes that will generate code for client-side validation also.</span></span> <span data-ttu-id="713da-191">Poniższy wyróżniony kod przedstawia sprawdzanie `Create` poprawności modelu w metodzie.</span><span class="sxs-lookup"><span data-stu-id="713da-191">The following highlighted code shows the model validation check in the `Create` method.</span></span>

[!code-csharp[](intro/samples/cu/Controllers/StudentsController.cs?name=snippet_Create&highlight=8)]

<span data-ttu-id="713da-192">Zmień datę na prawidłową wartość i kliknij przycisk **Utwórz,** aby nowy uczeń pojawił się na stronie **Indeks.**</span><span class="sxs-lookup"><span data-stu-id="713da-192">Change the date to a valid value and click **Create** to see the new student appear in the **Index** page.</span></span>

## <a name="update-the-edit-page"></a><span data-ttu-id="713da-193">Aktualizowanie strony edycji</span><span class="sxs-lookup"><span data-stu-id="713da-193">Update the Edit page</span></span>

<span data-ttu-id="713da-194">W *StudentController.cs*, HttpGet `Edit` metoda (jeden bez `HttpPost` atrybutu) używa `SingleOrDefaultAsync` metody do pobierania wybranej jednostki Student, jak widać w `Details` metodzie.</span><span class="sxs-lookup"><span data-stu-id="713da-194">In *StudentController.cs*, the HttpGet `Edit` method (the one without the `HttpPost` attribute) uses the `SingleOrDefaultAsync` method to retrieve the selected Student entity, as you saw in the `Details` method.</span></span> <span data-ttu-id="713da-195">Nie trzeba zmieniać tej metody.</span><span class="sxs-lookup"><span data-stu-id="713da-195">You don't need to change this method.</span></span>

### <a name="recommended-httppost-edit-code-read-and-update"></a><span data-ttu-id="713da-196">Zalecany kod edycji HttpPost: Czytaj i aktualizuj</span><span class="sxs-lookup"><span data-stu-id="713da-196">Recommended HttpPost Edit code: Read and update</span></span>

<span data-ttu-id="713da-197">Zastąp metodę akcji Edycja HttpPost następującym kodem.</span><span class="sxs-lookup"><span data-stu-id="713da-197">Replace the HttpPost Edit action method with the following code.</span></span>

[!code-csharp[](intro/samples/cu/Controllers/StudentsController.cs?name=snippet_ReadFirst)]

<span data-ttu-id="713da-198">Te zmiany implementują najlepsze rozwiązania w zakresie zabezpieczeń, aby zapobiec nadpostowywania.</span><span class="sxs-lookup"><span data-stu-id="713da-198">These changes implement a security best practice to prevent overposting.</span></span> <span data-ttu-id="713da-199">Rusztowania generowane `Bind` atrybut i dodał jednostki utworzone przez spinacza modelu `Modified` do jednostki zestaw z flagą.</span><span class="sxs-lookup"><span data-stu-id="713da-199">The scaffolder generated a `Bind` attribute and added the entity created by the model binder to the entity set with a `Modified` flag.</span></span> <span data-ttu-id="713da-200">Ten kod nie jest zalecane w `Bind` wielu scenariuszach, ponieważ atrybut czyści wszystkie istniejące `Include` dane w polach nie wymienionych w parametrze.</span><span class="sxs-lookup"><span data-stu-id="713da-200">That code isn't recommended for many scenarios because the `Bind` attribute clears out any pre-existing data in fields not listed in the `Include` parameter.</span></span>

<span data-ttu-id="713da-201">Nowy kod odczytuje istniejącą `TryUpdateModel` encję i wywołuje zaktualizowanie pól w pobranej encji [na podstawie danych wejściowych użytkownika w opublikowanych danych formularza](xref:mvc/models/model-binding).</span><span class="sxs-lookup"><span data-stu-id="713da-201">The new code reads the existing entity and calls `TryUpdateModel` to update fields in the retrieved entity [based on user input in the posted form data](xref:mvc/models/model-binding).</span></span> <span data-ttu-id="713da-202">Funkcja automatycznego śledzenia zmian w `Modified` ramach encji ustawia flagę na polach, które są zmieniane przez dane wejściowe formularza.</span><span class="sxs-lookup"><span data-stu-id="713da-202">The Entity Framework's automatic change tracking sets the `Modified` flag on the fields that are changed by form input.</span></span> <span data-ttu-id="713da-203">Gdy `SaveChanges` metoda jest wywoływana, Entity Framework tworzy instrukcje SQL, aby zaktualizować wiersz bazy danych.</span><span class="sxs-lookup"><span data-stu-id="713da-203">When the `SaveChanges` method is called, the Entity Framework creates SQL statements to update the database row.</span></span> <span data-ttu-id="713da-204">Konflikty współbieżności są ignorowane i tylko kolumny tabel, które zostały zaktualizowane przez użytkownika są aktualizowane w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="713da-204">Concurrency conflicts are ignored, and only the table columns that were updated by the user are updated in the database.</span></span> <span data-ttu-id="713da-205">(W późniejszym samouczku pokazano, jak obsługiwać konflikty współbieżności.</span><span class="sxs-lookup"><span data-stu-id="713da-205">(A later tutorial shows how to handle concurrency conflicts.)</span></span>

<span data-ttu-id="713da-206">Najlepszym rozwiązaniem, aby zapobiec nadpostowaniu, pola, które mają być aktualizowane przez **edit** strony są umieszczane na białej `TryUpdateModel` liście w parametrach.</span><span class="sxs-lookup"><span data-stu-id="713da-206">As a best practice to prevent overposting, the fields that you want to be updateable by the **Edit** page are whitelisted in the `TryUpdateModel` parameters.</span></span> <span data-ttu-id="713da-207">(Pusty ciąg poprzedzający listę pól na liście parametrów jest dla prefiksu do użycia z nazwami pól formularza). Obecnie nie ma żadnych dodatkowych pól, które chronisz, ale lista pól, które mają być powiązane spinacza modelu, gwarantuje, że jeśli dodasz pola do modelu danych w przyszłości, są one automatycznie chronione, dopóki nie zostanie jawnie dodane tutaj.</span><span class="sxs-lookup"><span data-stu-id="713da-207">(The empty string preceding the list of fields in the parameter list is for a prefix to use with the form fields names.) Currently there are no extra fields that you're protecting, but listing the fields that you want the model binder to bind ensures that if you add fields to the data model in the future, they're automatically protected until you explicitly add them here.</span></span>

<span data-ttu-id="713da-208">W wyniku tych zmian podpis metody HttpPost `Edit` metody jest taka sama `Edit` jak HttpGet metody; w związku z tym zmieniono nazwę metody `EditPost`.</span><span class="sxs-lookup"><span data-stu-id="713da-208">As a result of these changes, the method signature of the HttpPost `Edit` method is the same as the HttpGet `Edit` method; therefore you've renamed the method `EditPost`.</span></span>

### <a name="alternative-httppost-edit-code-create-and-attach"></a><span data-ttu-id="713da-209">Alternatywny kod edycji HttpPost: Tworzenie i dołączanie</span><span class="sxs-lookup"><span data-stu-id="713da-209">Alternative HttpPost Edit code: Create and attach</span></span>

<span data-ttu-id="713da-210">Zalecany kod edycji HttpPost zapewnia, że tylko zmienione kolumny są aktualizowane i zachowuje dane we właściwościach, które nie mają być uwzględnione dla powiązania modelu.</span><span class="sxs-lookup"><span data-stu-id="713da-210">The recommended HttpPost edit code ensures that only changed columns get updated and preserves data in properties that you don't want included for model binding.</span></span> <span data-ttu-id="713da-211">Jednak podejście do odczytu wymaga odczytu dodatkowej bazy danych i może spowodować bardziej złożony kod do obsługi konfliktów współbieżności.</span><span class="sxs-lookup"><span data-stu-id="713da-211">However, the read-first approach requires an extra database read, and can result in more complex code for handling concurrency conflicts.</span></span> <span data-ttu-id="713da-212">Alternatywą jest dołączenie jednostki utworzonej przez spinacza modelu do kontekstu EF i oznaczenie go jako zmodyfikowanego.</span><span class="sxs-lookup"><span data-stu-id="713da-212">An alternative is to attach an entity created by the model binder to the EF context and mark it as modified.</span></span> <span data-ttu-id="713da-213">(Nie aktualizuj projektu za pomocą tego kodu, jest on wyświetlany tylko w celu zilustrowania opcjonalnego podejścia).</span><span class="sxs-lookup"><span data-stu-id="713da-213">(Don't update your project with this code, it's only shown to illustrate an optional approach.)</span></span>

[!code-csharp[](intro/samples/cu/Controllers/StudentsController.cs?name=snippet_CreateAndAttach)]

<span data-ttu-id="713da-214">Można użyć tego podejścia, gdy interfejs użytkownika strony sieci web zawiera wszystkie pola w jednostce i można zaktualizować dowolną z nich.</span><span class="sxs-lookup"><span data-stu-id="713da-214">You can use this approach when the web page UI includes all of the fields in the entity and can update any of them.</span></span>

<span data-ttu-id="713da-215">Szkieletowy kod używa metody create-and-attach, ale tylko `DbUpdateConcurrencyException` połowy wyjątków i zwraca 404 kody błędów.</span><span class="sxs-lookup"><span data-stu-id="713da-215">The scaffolded code uses the create-and-attach approach but only catches `DbUpdateConcurrencyException` exceptions and returns 404 error codes.</span></span>  <span data-ttu-id="713da-216">Pokazany przykład łapie wyjątek aktualizacji bazy danych i wyświetla komunikat o błędzie.</span><span class="sxs-lookup"><span data-stu-id="713da-216">The example shown catches any database update exception and displays an error message.</span></span>

### <a name="entity-states"></a><span data-ttu-id="713da-217">Państwa członkowskie encji</span><span class="sxs-lookup"><span data-stu-id="713da-217">Entity States</span></span>

<span data-ttu-id="713da-218">Kontekst bazy danych śledzi, czy jednostki w pamięci są zsynchronizowane z odpowiadającymi im wierszami w bazie danych, a te informacje określają, co się dzieje po wywołaniu `SaveChanges` metody.</span><span class="sxs-lookup"><span data-stu-id="713da-218">The database context keeps track of whether entities in memory are in sync with their corresponding rows in the database, and this information determines what happens when you call the `SaveChanges` method.</span></span> <span data-ttu-id="713da-219">Na przykład po przełknięciu `Add` nowej jednostki do metody stan `Added`tej encji jest ustawiony na .</span><span class="sxs-lookup"><span data-stu-id="713da-219">For example, when you pass a new entity to the `Add` method, that entity's state is set to `Added`.</span></span> <span data-ttu-id="713da-220">Następnie po wywołaniu `SaveChanges` metody kontekst bazy danych wydaje polecenie SQL INSERT.</span><span class="sxs-lookup"><span data-stu-id="713da-220">Then when you call the `SaveChanges` method, the database context issues a SQL INSERT command.</span></span>

<span data-ttu-id="713da-221">Jednostka może znajdować się w jednym z następujących stanów:</span><span class="sxs-lookup"><span data-stu-id="713da-221">An entity may be in one of the following states:</span></span>

* <span data-ttu-id="713da-222">`Added`.</span><span class="sxs-lookup"><span data-stu-id="713da-222">`Added`.</span></span> <span data-ttu-id="713da-223">Jednostka jeszcze nie istnieje w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="713da-223">The entity doesn't yet exist in the database.</span></span> <span data-ttu-id="713da-224">Metoda `SaveChanges` wystawia instrukcję INSERT.</span><span class="sxs-lookup"><span data-stu-id="713da-224">The `SaveChanges` method issues an INSERT statement.</span></span>

* <span data-ttu-id="713da-225">`Unchanged`.</span><span class="sxs-lookup"><span data-stu-id="713da-225">`Unchanged`.</span></span> <span data-ttu-id="713da-226">Metoda nie musi być wykonywana `SaveChanges` z tą jednostką za pomocą metody.</span><span class="sxs-lookup"><span data-stu-id="713da-226">Nothing needs to be done with this entity by the `SaveChanges` method.</span></span> <span data-ttu-id="713da-227">Podczas odczytywania jednostki z bazy danych, jednostka rozpoczyna się od tego stanu.</span><span class="sxs-lookup"><span data-stu-id="713da-227">When you read an entity from the database, the entity starts out with this status.</span></span>

* <span data-ttu-id="713da-228">`Modified`.</span><span class="sxs-lookup"><span data-stu-id="713da-228">`Modified`.</span></span> <span data-ttu-id="713da-229">Niektóre lub wszystkie wartości właściwości jednostki zostały zmodyfikowane.</span><span class="sxs-lookup"><span data-stu-id="713da-229">Some or all of the entity's property values have been modified.</span></span> <span data-ttu-id="713da-230">Metoda `SaveChanges` wystawia instrukcję UPDATE.</span><span class="sxs-lookup"><span data-stu-id="713da-230">The `SaveChanges` method issues an UPDATE statement.</span></span>

* <span data-ttu-id="713da-231">`Deleted`.</span><span class="sxs-lookup"><span data-stu-id="713da-231">`Deleted`.</span></span> <span data-ttu-id="713da-232">Encja została oznaczona do usunięcia.</span><span class="sxs-lookup"><span data-stu-id="713da-232">The entity has been marked for deletion.</span></span> <span data-ttu-id="713da-233">Metoda `SaveChanges` wystawia instrukcję DELETE.</span><span class="sxs-lookup"><span data-stu-id="713da-233">The `SaveChanges` method issues a DELETE statement.</span></span>

* <span data-ttu-id="713da-234">`Detached`.</span><span class="sxs-lookup"><span data-stu-id="713da-234">`Detached`.</span></span> <span data-ttu-id="713da-235">Jednostka nie jest śledzona przez kontekst bazy danych.</span><span class="sxs-lookup"><span data-stu-id="713da-235">The entity isn't being tracked by the database context.</span></span>

<span data-ttu-id="713da-236">W aplikacji klasycznej zmiany stanu są zazwyczaj ustawiane automatycznie.</span><span class="sxs-lookup"><span data-stu-id="713da-236">In a desktop application, state changes are typically set automatically.</span></span> <span data-ttu-id="713da-237">Odczytasz encję i wprowadzać zmiany w niektórych jej wartościach właściwości.</span><span class="sxs-lookup"><span data-stu-id="713da-237">You read an entity and make changes to some of its property values.</span></span> <span data-ttu-id="713da-238">Powoduje to, że jego stan `Modified`jednostki jest automatycznie zmieniany na .</span><span class="sxs-lookup"><span data-stu-id="713da-238">This causes its entity state to automatically be changed to `Modified`.</span></span> <span data-ttu-id="713da-239">Następnie podczas `SaveChanges`wywoływania, Entity Framework generuje instrukcję SQL UPDATE, która aktualizuje tylko rzeczywiste właściwości, które zostały zmienione.</span><span class="sxs-lookup"><span data-stu-id="713da-239">Then when you call `SaveChanges`, the Entity Framework generates a SQL UPDATE statement that updates only the actual properties that you changed.</span></span>

<span data-ttu-id="713da-240">W aplikacji sieci `DbContext` web, która początkowo odczytuje jednostkę i wyświetla jej dane do edycji jest usuwany po renderowaniu strony.</span><span class="sxs-lookup"><span data-stu-id="713da-240">In a web app, the `DbContext` that initially reads an entity and displays its data to be edited is disposed after a page is rendered.</span></span> <span data-ttu-id="713da-241">Gdy wywoływana `Edit` jest metoda akcji HttpPost, zostanie wykonane nowe żądanie `DbContext`sieci web i pojawi się nowe wystąpienie pliku .</span><span class="sxs-lookup"><span data-stu-id="713da-241">When the HttpPost `Edit` action method is called,  a new web request is made and you have a new instance of the `DbContext`.</span></span> <span data-ttu-id="713da-242">Jeśli ponownie odczytasz jednostkę w tym nowym kontekście, symulujesz przetwarzanie pulpitu.</span><span class="sxs-lookup"><span data-stu-id="713da-242">If you re-read the entity in that new context, you simulate desktop processing.</span></span>

<span data-ttu-id="713da-243">Ale jeśli nie chcesz wykonać operację odczytu dodatkowego, należy użyć obiektu jednostki utworzone przez spinacza modelu.</span><span class="sxs-lookup"><span data-stu-id="713da-243">But if you don't want to do the extra read operation, you have to use the entity object created by the model binder.</span></span>  <span data-ttu-id="713da-244">Najprostszym sposobem, aby to zrobić, jest ustawienie stanu jednostki na Zmodyfikowany, tak jak odbywa się to w alternatywnym kodzie edycji HttpPost pokazanym wcześniej.</span><span class="sxs-lookup"><span data-stu-id="713da-244">The simplest way to do this is to set the entity state to Modified as is done in the alternative HttpPost Edit code shown earlier.</span></span> <span data-ttu-id="713da-245">Następnie podczas `SaveChanges`wywoływania, Entity Framework aktualizuje wszystkie kolumny wiersza bazy danych, ponieważ kontekst nie ma sposobu, aby wiedzieć, które właściwości zostały zmienione.</span><span class="sxs-lookup"><span data-stu-id="713da-245">Then when you call `SaveChanges`, the Entity Framework updates all columns of the database row, because the context has no way to know which properties you changed.</span></span>

<span data-ttu-id="713da-246">Jeśli chcesz uniknąć podejścia do odczytu, ale chcesz również instrukcji SQL UPDATE zaktualizować tylko pola, które użytkownik faktycznie zmienił, kod jest bardziej złożony.</span><span class="sxs-lookup"><span data-stu-id="713da-246">If you want to avoid the read-first approach, but you also want the SQL UPDATE statement to update only the fields that the user actually changed, the code is more complex.</span></span> <span data-ttu-id="713da-247">Musisz zapisać oryginalne wartości w jakiś sposób (na przykład za pomocą ukrytych pól), `Edit` tak aby były one dostępne, gdy wywoływana jest metoda HttpPost.</span><span class="sxs-lookup"><span data-stu-id="713da-247">You have to save the original values in some way (such as by using hidden fields) so that they're available when the HttpPost `Edit` method is called.</span></span> <span data-ttu-id="713da-248">Następnie można utworzyć jednostkę Student przy użyciu `Attach` oryginalnych wartości, wywołać metodę z tą oryginalną wersją encji, zaktualizować wartości encji do nowych wartości, a następnie wywołać `SaveChanges`.</span><span class="sxs-lookup"><span data-stu-id="713da-248">Then you can create a Student entity using the original values, call the `Attach` method with that original version of the entity, update the entity's values to the new values, and then call `SaveChanges`.</span></span>

### <a name="test-the-edit-page"></a><span data-ttu-id="713da-249">Testowanie strony Edycji</span><span class="sxs-lookup"><span data-stu-id="713da-249">Test the Edit page</span></span>

<span data-ttu-id="713da-250">Uruchom aplikację, wybierz kartę **Uczniowie,** a następnie kliknij **hiperłącze Edycja.**</span><span class="sxs-lookup"><span data-stu-id="713da-250">Run the app, select the **Students** tab, then click an **Edit** hyperlink.</span></span>

![Uczniowie edytują stronę](crud/_static/student-edit.png)

<span data-ttu-id="713da-252">Zmień niektóre dane i kliknij przycisk **Zapisz**.</span><span class="sxs-lookup"><span data-stu-id="713da-252">Change some of the data and click **Save**.</span></span> <span data-ttu-id="713da-253">Zostanie otwarta strona **Indeks** i zobaczysz zmienione dane.</span><span class="sxs-lookup"><span data-stu-id="713da-253">The **Index** page opens and you see the changed data.</span></span>

## <a name="update-the-delete-page"></a><span data-ttu-id="713da-254">Aktualizowanie strony Usuwanie</span><span class="sxs-lookup"><span data-stu-id="713da-254">Update the Delete page</span></span>

<span data-ttu-id="713da-255">W *StudentController.cs*, kod szablonu dla `Delete` HttpGet metoda `SingleOrDefaultAsync` używa metody do pobierania wybranej jednostki Student, jak widać w Szczegóły i Edytuj metody.</span><span class="sxs-lookup"><span data-stu-id="713da-255">In *StudentController.cs*, the template code for the HttpGet `Delete` method uses the `SingleOrDefaultAsync` method to retrieve the selected Student entity, as you saw in the Details and Edit methods.</span></span> <span data-ttu-id="713da-256">Jednak aby zaimplementować niestandardowy `SaveChanges` komunikat o błędzie, gdy wywołanie nie powiedzie się, należy dodać niektóre funkcje do tej metody i jej odpowiedniego widoku.</span><span class="sxs-lookup"><span data-stu-id="713da-256">However, to implement a custom error message when the call to `SaveChanges` fails, you'll add some functionality to this method and its corresponding view.</span></span>

<span data-ttu-id="713da-257">Jak widać dla aktualizacji i tworzenia operacji, operacje usuwania wymagają dwóch metod akcji.</span><span class="sxs-lookup"><span data-stu-id="713da-257">As you saw for update and create operations, delete operations require two action methods.</span></span> <span data-ttu-id="713da-258">Metoda, która jest wywoływana w odpowiedzi na żądanie GET wyświetla widok, który daje użytkownikowi możliwość zatwierdzenia lub anulowania operacji usuwania.</span><span class="sxs-lookup"><span data-stu-id="713da-258">The method that's called in response to a GET request displays a view that gives the user a chance to approve or cancel the delete operation.</span></span> <span data-ttu-id="713da-259">Jeśli użytkownik go zatwierdzi, zostanie utworzone żądanie POST.</span><span class="sxs-lookup"><span data-stu-id="713da-259">If the user approves it, a POST request is created.</span></span> <span data-ttu-id="713da-260">W takim przypadku wywoływana jest metoda HttpPost, `Delete` a następnie ta metoda faktycznie wykonuje operację usuwania.</span><span class="sxs-lookup"><span data-stu-id="713da-260">When that happens, the HttpPost `Delete` method is called and then that method actually performs the delete operation.</span></span>

<span data-ttu-id="713da-261">Dodasz blok try-catch do metody `Delete` HttpPost, aby obsłużyć wszelkie błędy, które mogą wystąpić podczas aktualizacji bazy danych.</span><span class="sxs-lookup"><span data-stu-id="713da-261">You'll add a try-catch block to the HttpPost `Delete` method to handle any errors that might occur when the database is updated.</span></span> <span data-ttu-id="713da-262">Jeśli wystąpi błąd, HttpPost Delete metoda wywołuje HttpGet Delete metody, przekazując go parametr, który wskazuje, że wystąpił błąd.</span><span class="sxs-lookup"><span data-stu-id="713da-262">If an error occurs, the HttpPost Delete method calls the HttpGet Delete method, passing it a parameter that indicates that an error has occurred.</span></span> <span data-ttu-id="713da-263">HttpGet Delete Metoda następnie ponownie emituje stronę potwierdzenia wraz z komunikatem o błędzie, dając użytkownikowi możliwość anulowania lub spróbuj ponownie.</span><span class="sxs-lookup"><span data-stu-id="713da-263">The HttpGet Delete method then redisplays the confirmation page along with the error message, giving the user an opportunity to cancel or try again.</span></span>

<span data-ttu-id="713da-264">Zastąp `Delete` httpget metody akcji z następującym kodem, który zarządza raportowania błędów.</span><span class="sxs-lookup"><span data-stu-id="713da-264">Replace the HttpGet `Delete` action method with the following code, which manages error reporting.</span></span>

[!code-csharp[](intro/samples/cu/Controllers/StudentsController.cs?name=snippet_DeleteGet&highlight=1,9,16-21)]

<span data-ttu-id="713da-265">Ten kod akceptuje parametr opcjonalny, który wskazuje, czy metoda została wywołana po nieuchronienie zmian.</span><span class="sxs-lookup"><span data-stu-id="713da-265">This code accepts an optional parameter that indicates whether the method was called after a failure to save changes.</span></span> <span data-ttu-id="713da-266">Ten parametr jest false, `Delete` gdy HttpGet metoda jest wywoływana bez poprzedniego błędu.</span><span class="sxs-lookup"><span data-stu-id="713da-266">This parameter is false when the HttpGet `Delete` method is called without a previous failure.</span></span> <span data-ttu-id="713da-267">Gdy jest wywoływana przez `Delete` metodę HttpPost w odpowiedzi na błąd aktualizacji bazy danych, parametr jest true i komunikat o błędzie jest przekazywany do widoku.</span><span class="sxs-lookup"><span data-stu-id="713da-267">When it's called by the HttpPost `Delete` method in response to a database update error, the parameter is true and an error message is passed to the view.</span></span>

### <a name="the-read-first-approach-to-httppost-delete"></a><span data-ttu-id="713da-268">Pierwsze podejście do httppost delete</span><span class="sxs-lookup"><span data-stu-id="713da-268">The read-first approach to HttpPost Delete</span></span>

<span data-ttu-id="713da-269">Zastąp `Delete` metodę akcji `DeleteConfirmed`HttpPost (o nazwie) następującym kodem, który wykonuje operację usuwania rzeczywistego i wychwytuje błędy aktualizacji bazy danych.</span><span class="sxs-lookup"><span data-stu-id="713da-269">Replace the HttpPost `Delete` action method (named `DeleteConfirmed`) with the following code, which performs the actual delete operation and catches any database update errors.</span></span>

[!code-csharp[](intro/samples/cu/Controllers/StudentsController.cs?name=snippet_DeleteWithReadFirst&highlight=6-9,11-12,16-21)]

<span data-ttu-id="713da-270">Ten kod pobiera wybranej jednostki, a następnie wywołuje `Remove` metodę, aby ustawić stan jednostki na `Deleted`.</span><span class="sxs-lookup"><span data-stu-id="713da-270">This code retrieves the selected entity, then calls the `Remove` method to set the entity's status to `Deleted`.</span></span> <span data-ttu-id="713da-271">Po `SaveChanges` wywołaniu jest generowane polecenie SQL DELETE.</span><span class="sxs-lookup"><span data-stu-id="713da-271">When `SaveChanges` is called, a SQL DELETE command is generated.</span></span>

### <a name="the-create-and-attach-approach-to-httppost-delete"></a><span data-ttu-id="713da-272">Podejście do tworzenia i dołączania do httppost delete</span><span class="sxs-lookup"><span data-stu-id="713da-272">The create-and-attach approach to HttpPost Delete</span></span>

<span data-ttu-id="713da-273">Jeśli priorytetem jest poprawa wydajności w aplikacji o dużej objętości, można uniknąć niepotrzebnego zapytania SQL, tworząc wystąpienie jednostki Student przy użyciu tylko wartości klucza podstawowego, a następnie ustawiając stan jednostki na `Deleted`.</span><span class="sxs-lookup"><span data-stu-id="713da-273">If improving performance in a high-volume application is a priority, you could avoid an unnecessary SQL query by instantiating a Student entity using only the primary key value and then setting the entity state to `Deleted`.</span></span> <span data-ttu-id="713da-274">To wszystko, czego potrzebuje entity framework w celu usunięcia jednostki.</span><span class="sxs-lookup"><span data-stu-id="713da-274">That's all that the Entity Framework needs in order to delete the entity.</span></span> <span data-ttu-id="713da-275">(Nie umieszczaj tego kodu w projekcie, ale jest tutaj tylko po to, aby zilustrować alternatywę).</span><span class="sxs-lookup"><span data-stu-id="713da-275">(Don't put this code in your project; it's here just to illustrate an alternative.)</span></span>

[!code-csharp[](intro/samples/cu/Controllers/StudentsController.cs?name=snippet_DeleteWithoutReadFirst&highlight=7-8)]

<span data-ttu-id="713da-276">Jeśli jednostka ma powiązane dane, które również powinny zostać usunięte, upewnij się, że kaskadowe usuwanie jest skonfigurowane w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="713da-276">If the entity has related data that should also be deleted, make sure that cascade delete is configured in the database.</span></span> <span data-ttu-id="713da-277">Dzięki takiemu podejściu do usuwania encji EF może nie zdawać sobie sprawy, że istnieją powiązane jednostki do usunięcia.</span><span class="sxs-lookup"><span data-stu-id="713da-277">With this approach to entity deletion, EF might not realize there are related entities to be deleted.</span></span>

### <a name="update-the-delete-view"></a><span data-ttu-id="713da-278">Aktualizowanie widoku Usuń</span><span class="sxs-lookup"><span data-stu-id="713da-278">Update the Delete view</span></span>

<span data-ttu-id="713da-279">W *obszarze Widoki/Student/Delete.cshtml*dodaj komunikat o błędzie między nagłówkiem h2 a nagłówkiem h3, jak pokazano w poniższym przykładzie:</span><span class="sxs-lookup"><span data-stu-id="713da-279">In *Views/Student/Delete.cshtml*, add an error message between the h2 heading and the h3 heading, as shown in the following example:</span></span>

[!code-html[](intro/samples/cu/Views/Students/Delete.cshtml?range=7-9&highlight=2)]

<span data-ttu-id="713da-280">Uruchom aplikację, wybierz kartę **Uczniowie** i kliknij usuń **hiperłącze:**</span><span class="sxs-lookup"><span data-stu-id="713da-280">Run the app, select the **Students** tab, and click a **Delete** hyperlink:</span></span>

![Usuń stronę potwierdzenia](crud/_static/student-delete.png)

<span data-ttu-id="713da-282">Kliknij **pozycję Usuń**.</span><span class="sxs-lookup"><span data-stu-id="713da-282">Click **Delete**.</span></span> <span data-ttu-id="713da-283">Strona Indeks jest wyświetlana bez usuniętego ucznia.</span><span class="sxs-lookup"><span data-stu-id="713da-283">The Index page is displayed without the deleted student.</span></span> <span data-ttu-id="713da-284">(Zobaczysz przykład kodu obsługi błędów w akcji w samouczku współbieżności).)</span><span class="sxs-lookup"><span data-stu-id="713da-284">(You'll see an example of the error handling code in action in the concurrency tutorial.)</span></span>

## <a name="close-database-connections"></a><span data-ttu-id="713da-285">Zamykanie połączeń z bazą danych</span><span class="sxs-lookup"><span data-stu-id="713da-285">Close database connections</span></span>

<span data-ttu-id="713da-286">Aby zwolnić zasoby, które przechowuje połączenie z bazą danych, wystąpienie kontekstu musi zostać usunięte tak szybko, jak to możliwe, gdy skończysz z nim.</span><span class="sxs-lookup"><span data-stu-id="713da-286">To free up the resources that a database connection holds, the context instance must be disposed as soon as possible when you are done with it.</span></span> <span data-ttu-id="713da-287">Wbudowany [iniekcja zależności](../../fundamentals/dependency-injection.md) ASP.NET Core zajmuje się tym zadaniem.</span><span class="sxs-lookup"><span data-stu-id="713da-287">The ASP.NET Core built-in [dependency injection](../../fundamentals/dependency-injection.md) takes care of that task for you.</span></span>

<span data-ttu-id="713da-288">W *Startup.cs*, należy wywołać [AddDbContext metody rozszerzenia](https://github.com/aspnet/EntityFrameworkCore/blob/03bcb5122e3f577a84498545fcf130ba79a3d987/src/Microsoft.EntityFrameworkCore/EntityFrameworkServiceCollectionExtensions.cs) do inicjowania obsługi administracyjnej `DbContext` klasy w kontenerze ASP.NET Core DI.</span><span class="sxs-lookup"><span data-stu-id="713da-288">In *Startup.cs*, you call the [AddDbContext extension method](https://github.com/aspnet/EntityFrameworkCore/blob/03bcb5122e3f577a84498545fcf130ba79a3d987/src/Microsoft.EntityFrameworkCore/EntityFrameworkServiceCollectionExtensions.cs) to provision the `DbContext` class in the ASP.NET Core DI container.</span></span> <span data-ttu-id="713da-289">Ta metoda domyślnie `Scoped` ustawia okres istnienia usługi.</span><span class="sxs-lookup"><span data-stu-id="713da-289">That method sets the service lifetime to `Scoped` by default.</span></span> <span data-ttu-id="713da-290">`Scoped`oznacza, że okres istnienia obiektu kontekstu pokrywa `Dispose` się z czasem życia żądania sieci web, a metoda zostanie wywołana automatycznie na końcu żądania sieci web.</span><span class="sxs-lookup"><span data-stu-id="713da-290">`Scoped` means the context object lifetime coincides with the web request life time, and the `Dispose` method will be called automatically at the end of the web request.</span></span>

## <a name="handle-transactions"></a><span data-ttu-id="713da-291">Obsługa transakcji</span><span class="sxs-lookup"><span data-stu-id="713da-291">Handle transactions</span></span>

<span data-ttu-id="713da-292">Domyślnie Entity Framework niejawnie implementuje transakcje.</span><span class="sxs-lookup"><span data-stu-id="713da-292">By default the Entity Framework implicitly implements transactions.</span></span> <span data-ttu-id="713da-293">W scenariuszach, w których można wprowadzić zmiany `SaveChanges`w wielu wierszach lub tabelach, a następnie wywołać , Entity Framework automatycznie upewnia się, że wszystkie zmiany zakończy się pomyślnie lub wszystkie one nie powiedzie się.</span><span class="sxs-lookup"><span data-stu-id="713da-293">In scenarios where you make changes to multiple rows or tables and then call `SaveChanges`, the Entity Framework automatically makes sure that either all of your changes succeed or they all fail.</span></span> <span data-ttu-id="713da-294">Jeśli niektóre zmiany są wykonywane najpierw, a następnie występuje błąd, zmiany te są automatycznie przywracane.</span><span class="sxs-lookup"><span data-stu-id="713da-294">If some changes are done first and then an error happens, those changes are automatically rolled back.</span></span> <span data-ttu-id="713da-295">W przypadku scenariuszy, w których potrzebujesz większej kontroli — na przykład, jeśli chcesz uwzględnić operacje wykonywane poza platformą Encji w transakcji — zobacz [Transakcje](/ef/core/saving/transactions).</span><span class="sxs-lookup"><span data-stu-id="713da-295">For scenarios where you need more control -- for example, if you want to include operations done outside of Entity Framework in a transaction -- see [Transactions](/ef/core/saving/transactions).</span></span>

## <a name="no-tracking-queries"></a><span data-ttu-id="713da-296">Zapytania bez śledzenia</span><span class="sxs-lookup"><span data-stu-id="713da-296">No-tracking queries</span></span>

<span data-ttu-id="713da-297">Gdy kontekst bazy danych pobiera wiersze tabeli i tworzy obiekty encji, które je reprezentują, domyślnie śledzi, czy jednostki w pamięci są zsynchronizowane z tym, co znajduje się w bazie danych.</span><span class="sxs-lookup"><span data-stu-id="713da-297">When a database context retrieves table rows and creates entity objects that represent them, by default it keeps track of whether the entities in memory are in sync with what's in the database.</span></span> <span data-ttu-id="713da-298">Dane w pamięci działa jako pamięć podręczna i jest używany podczas aktualizacji jednostki.</span><span class="sxs-lookup"><span data-stu-id="713da-298">The data in memory acts as a cache and is used when you update an entity.</span></span> <span data-ttu-id="713da-299">Buforowanie to jest często niepotrzebne w aplikacji sieci web, ponieważ wystąpienia kontekstu są zazwyczaj krótkotrwałe (nowy jest tworzony i usuwany dla każdego żądania), a kontekst, który odczytuje jednostkę, jest zazwyczaj usuwany przed ponownym użyciem tej jednostki.</span><span class="sxs-lookup"><span data-stu-id="713da-299">This caching is often unnecessary in a web application because context instances are typically short-lived (a new one is created and disposed for each request) and the context that reads an entity is typically disposed before that entity is used again.</span></span>

<span data-ttu-id="713da-300">Można wyłączyć śledzenie obiektów jednostki w `AsNoTracking` pamięci, wywołując metodę.</span><span class="sxs-lookup"><span data-stu-id="713da-300">You can disable tracking of entity objects in memory by calling the `AsNoTracking` method.</span></span> <span data-ttu-id="713da-301">Typowe scenariusze, w których można to zrobić, obejmują następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="713da-301">Typical scenarios in which you might want to do that include the following:</span></span>

* <span data-ttu-id="713da-302">W okresie istnienia kontekstu nie trzeba aktualizować żadnych jednostek i nie trzeba EF [automatycznie ładować właściwości nawigacji z jednostek pobranych przez oddzielne kwerendy](read-related-data.md).</span><span class="sxs-lookup"><span data-stu-id="713da-302">During the context lifetime you don't need to update any entities, and you don't need EF to [automatically load navigation properties with  entities retrieved by separate queries](read-related-data.md).</span></span> <span data-ttu-id="713da-303">Często te warunki są spełnione w metodach akcji HttpGet kontrolera.</span><span class="sxs-lookup"><span data-stu-id="713da-303">Frequently these conditions are met in a controller's HttpGet action methods.</span></span>

* <span data-ttu-id="713da-304">Uruchamiasz kwerendę, która pobiera dużą ilość danych, a tylko niewielka część zwróconych danych zostanie zaktualizowana.</span><span class="sxs-lookup"><span data-stu-id="713da-304">You are running a query that retrieves a large volume of data, and only a small portion of the returned data will be updated.</span></span> <span data-ttu-id="713da-305">Może być bardziej wydajne, aby wyłączyć śledzenie dla dużych kwerendy i uruchomić kwerendę później dla kilku jednostek, które muszą zostać zaktualizowane.</span><span class="sxs-lookup"><span data-stu-id="713da-305">It may be more efficient to turn off tracking for the large query, and run a query later for the few entities that need to be updated.</span></span>

* <span data-ttu-id="713da-306">Chcesz dołączyć jednostkę w celu jej aktualizacji, ale wcześniej pobrano tę samą jednostkę w innym celu.</span><span class="sxs-lookup"><span data-stu-id="713da-306">You want to attach an entity in order to update it, but earlier you retrieved the same entity for a different purpose.</span></span> <span data-ttu-id="713da-307">Ponieważ jednostka jest już śledzona przez kontekst bazy danych, nie można dołączyć encji, którą chcesz zmienić.</span><span class="sxs-lookup"><span data-stu-id="713da-307">Because the entity is already being tracked by the database context, you can't attach the entity that you want to change.</span></span> <span data-ttu-id="713da-308">Jednym ze sposobów obsługi tej `AsNoTracking` sytuacji jest wywołanie wcześniejszej kwerendy.</span><span class="sxs-lookup"><span data-stu-id="713da-308">One way to handle this situation is to call `AsNoTracking` on the earlier query.</span></span>

<span data-ttu-id="713da-309">Aby uzyskać więcej informacji, zobacz [Śledzenie a brak śledzenia](/ef/core/querying/tracking).</span><span class="sxs-lookup"><span data-stu-id="713da-309">For more information, see [Tracking vs. No-Tracking](/ef/core/querying/tracking).</span></span>

## <a name="get-the-code"></a><span data-ttu-id="713da-310">Uzyskiwanie kodu</span><span class="sxs-lookup"><span data-stu-id="713da-310">Get the code</span></span>

[<span data-ttu-id="713da-311">Pobierz lub wyświetl ukończoną aplikację.</span><span class="sxs-lookup"><span data-stu-id="713da-311">Download or view the completed application.</span></span>](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/data/ef-mvc/intro/samples/cu-final)

## <a name="next-steps"></a><span data-ttu-id="713da-312">Następne kroki</span><span class="sxs-lookup"><span data-stu-id="713da-312">Next steps</span></span>

<span data-ttu-id="713da-313">W tym samouczku zostały wykonane następujące czynności:</span><span class="sxs-lookup"><span data-stu-id="713da-313">In this tutorial, you:</span></span>

> [!div class="checklist"]
> * <span data-ttu-id="713da-314">Dostosowano stronę Szczegóły</span><span class="sxs-lookup"><span data-stu-id="713da-314">Customized the Details page</span></span>
> * <span data-ttu-id="713da-315">Zaktualizowano stronę Tworzenie</span><span class="sxs-lookup"><span data-stu-id="713da-315">Updated the Create page</span></span>
> * <span data-ttu-id="713da-316">Zaktualizowano stronę Edycji</span><span class="sxs-lookup"><span data-stu-id="713da-316">Updated the Edit page</span></span>
> * <span data-ttu-id="713da-317">Zaktualizowano stronę Usuwanie</span><span class="sxs-lookup"><span data-stu-id="713da-317">Updated the Delete page</span></span>
> * <span data-ttu-id="713da-318">Zamknięte połączenia z bazą danych</span><span class="sxs-lookup"><span data-stu-id="713da-318">Closed database connections</span></span>

<span data-ttu-id="713da-319">Przejdź do następnego samouczka, aby dowiedzieć się, jak rozszerzyć funkcjonalność strony **Indeks,** dodając sortowanie, filtrowanie i stronicowanie.</span><span class="sxs-lookup"><span data-stu-id="713da-319">Advance to the next tutorial to learn how to expand the functionality of the **Index** page by adding sorting, filtering, and paging.</span></span>

> [!div class="nextstepaction"]
> [<span data-ttu-id="713da-320">Dalej: Sortowanie, filtrowanie i stronicowanie</span><span class="sxs-lookup"><span data-stu-id="713da-320">Next: Sorting, filtering, and paging</span></span>](sort-filter-page.md)
